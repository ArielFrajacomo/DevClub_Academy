# Auto-detect text files and normalize them to LF in the repository.
* text=auto

# Keep source files consistent across environments.
*.html text eol=lf
*.css text eol=lf
*.md text eol=lf
*.svg text eol=lf

# Treat image assets as binary files.
*.png binary 